[build]
  base = "/"
  command = "npm ci && npm install && node ./node_modules/vite/bin/vite.js build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173

[context.production.environment]
  NODE_ENV = "production"

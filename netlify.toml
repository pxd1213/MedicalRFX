[build]
  base = "/"
  command = "npm install --legacy-peer-deps && npm run build -- --max-old-space-size=4096"
  publish = "build"
  node = "16.18.0"

[build.environment]
  NODE_ENV = "production"
  CI = "false"

[dev]
  command = "npm start"
  port = 3000

[build]
  base = "/"
  command = "npm install vite@5.0.9 && npx vite build"
  publish = "build"
  node = "16.20.2"

[build.environment]
  NODE_ENV = "production"
  CI = "false"

[dev]
  command = "npm start"
  port = 3000

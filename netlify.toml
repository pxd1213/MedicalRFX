[build]
  base = "/"
  command = "npm run clean && npm install && npm run build"
  publish = "build"
  node = "16.20.2"

[dev]
  command = "npm start"
  port = 3000

[context.production.environment]
  NODE_ENV = "production"
  CI = "false"
